{
  "name": "odin-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "React source for the Odin dashboard. Builds to a single self-contained ../static/index.html — no CDN, no separate JS/CSS assets, matching how server.py serves that file directly.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "esbuild": "^0.28.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
